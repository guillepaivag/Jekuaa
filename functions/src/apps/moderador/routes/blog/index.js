const { Router } = require('express')
const router = Router()

// HABILITAR EL BLOG
router.put('/habilitar/:uid', )


// DESHABILITAR EL BLOG
router.put('/deshabilitar/:uid', )


module.exports = router